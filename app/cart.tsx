// TODO: this just a test file of modal

import { View, Text, Platform, FlatList } from "react-native";
import { StatusBar } from "expo-status-bar";

const CartScreen = () => {
  return (
    <View style={{ padding: 10 }}>
      <Text>Test Cart</Text>
    </View>
  );
};

export default CartScreen;
